# OCR 管线缺陷报告（基准测试暴露项汇总）

- 日期：2026-09-11
- 依据：三案例最近一次基准跑测（方案 A 口径基线，2026-09-11 行）与 `log/` 下 4 份跑测日志中产物与参考文本的逐条对比
- 范围声明：仅收录**管线缺陷**；测量口径问题（参考重复条目→幻影缺失）已修复并另记于 [review-reports/BENCH_SCORING_DUPLICATE_EXPECTED.md](../review-reports/BENCH_SCORING_DUPLICATE_EXPECTED.md)；语料噪音混入按设计是融合容忍度考核素材，不算缺陷
- 数据速览（语料 99.7/97.3/96.7，嵌字 21.5/18.2/17.6，容差 0.3s）

## D1 碎片化：单条字幕被拆成多段（嵌字链路，影响最大）

- **证据**：1:N 碎片化计数——glupov 10/22 条、moon 9/19 条、pierro 54/119 条；平均每条被拆 ~2~3 段。对齐文本相似度均值仅 0.43~0.49（产出段与参考条目只有部分重叠），是嵌字低分（17.6~21.5）的最大扣分项。
- **现象**：打字机式渐进文本随帧逐步显现，变化检测逐帧判定"变化"→ 每帧 OCR 只产出当前部分文本 → 相邻部分文本被切成独立段。
- **疑似根因**：`merge_frames` 的"前缀超集合并"依赖 run 连续性——一旦中间被空帧间隔打断（`empty_gap = max(interval×1.5, 0.8s)` 不够容纳打字机间隙）或相似基准失稳，渐进序列就断开成多段；相邻段合并（`merge_similar_adjacent`）对"文本互为前后缀/子串"的段未做拼接。
- **修复方向**：段级"包含关系拼接"（相邻且时间连续、文本互为前后缀的段合并取并集）；或把渐进检测从帧级提升到段级。
- **状态**：⬜ 待修复

## D2 段尾延伸过长：Δend 偏差显著大于 Δstart

- **证据**：moon Δstart p95 **1.24s** vs Δend p95 **5.62s**；pierro Δstart 1.90s vs Δend **9.80s**。阶段 1 帧级主边界精化（`eedcc3e`）已把段首压住，段尾未受益。
- **疑似根因**：空帧抖动容错窗口把段尾的空白帧/无关帧并入段尾（`end = 最后帧时间 + interval` 的顺延口径），且精化阶段对段尾的召回与段首不对等。
- **修复方向**：段尾同样做精化/回退到"最后一次非空帧"；复查空帧容错对段尾的顺延语义。
- **状态**：⬜ 待根因（先与 D1 的合并逻辑一并分析）

## D3 语料真实缺失 2 条（pierro，仅此案例）

- **证据**：方案 A 口径下仍有缺失——`[70] 派蒙 / 欸？全部吗？我知道你们说的「死之诅咒」，还有你和戴因斯雷布所中的「不死诅咒」……`、`[75] 「丑角」 / 请期待吧，那将是超乎你们想象的世界。`（产物中无对应文本）。
- **疑似根因**（三选一，待 `dev_debug` 帧落盘定位）：① 被相似度合并进相邻段（文本存在但非独立条目，对齐按整条匹配不到）；② worker 低置信度行过滤（`CONF_THRESHOLD=0.5`）整行丢弃；③ 该时段字幕位置偏离选区窗（语料片选区为单条 0–1272.3s 大区间）。
- **修复方向**：定位后再定——若①属 D1 的子症状；若②考虑分位置信度或按行恢复；若③需检查语料片选区。
- **状态**：⬜ 待根因

## D4 字符级识别错误（三案例轻度合计 34 条，CER 均值 0.003~0.028）

- **证据**（glupov 最差匹配 Top10，全部为同模式系统性错误）：
  - `「缟玛瑙」` → `[编玛瑙】/「编玛瑙]`——**同形近字 缟→编 贯穿该案例全部条目**；全角「」→半角 [ ] 混杂、右括号丢失（`队员」` → `队员`、`临时连长」` → `连队J临时连长`）。
  - 省略号缩短（`……` → `…`）、漏尾字、换行断句差异（"…本质区别" → "…本质区别"被折行拆开）。
- **定位**：PaddleOCR 识别精度为主（形近字 + 字幕框边缘括号），非合并/逻辑缺陷；worker 白名单保留全半角括号使变体原样透传（宽松归一化后 CER 已抹平，对融合影响有限——LLM 融合按语义对应，括号/省略号差异预期可容忍，待融合基准验证）。
- **修复方向**：低优先。可选：白名单内做全半角括号归一、或对已知角色头衔做词典校正；更实际的是接受现状并在融合链路验证其影响。
- **状态**：⬜ 接受/低优先

## D5 边界量化残差（帧间隔网格 ±0.5s）

- **证据**：0.3s 容差下 ≤容差占比——glupov 0%、moon 60%、pierro 6%。阶段 1 精化已把段首压至 Δstart p95 1.2~1.9s，残差主要来自 0.5s 采样网格与 D2 的段尾问题。
- **修复方向**：计划中的"变化检测二分精化"待办；但优先级低于 D1/D2——先把分段结构修对，边界精化才有意义。
- **状态**：⬜ 已有既有待办（二分精化）

## 非缺陷项（记录备查，不计入修复清单）

- **语料噪音行**（glupov 6 / moon 2 / pierro 51，pierro 占产出的 31%）：语料片含非字幕/无关画面文本，按口径不判败，是案例3 留给融合基准的容忍度素材；其规模同时提示融合 prompt 的上下文占用（OCR 全量入批）需在融合基准中实测。
- **glupov 嵌字 Δstart p95 = 16.35s 离群**：1:1 配对仅 10 条，p95=最大值（PR31 P2-2 已记录）；个别长段被 50% 重叠规则判 1:1 但实为错配，属指标呈现问题而非独立管线缺陷。
- **glupov 嵌字缺失 [18]**：参考条目文本为纯省略号（"……"）的超短行——疑似被行级清理/空文本语义过滤，属 D3 同类（超短/低信息行处理），归入 D3 一并根因。

## 修复优先级

1. **D1 碎片化**（嵌字链路质量的最大瓶颈，三案例均现）
2. **D2 段尾延伸**（与 D1 同属分段稳定性，可一并处理）
3. **D3 pierro 语料缺失 2 条 + 超短行缺失**（待 dev_debug 定位）
4. D5 二分精化（既有待办，殿后）
5. D4 字符精度（低优先，可留待融合基准验证影响后决定）
